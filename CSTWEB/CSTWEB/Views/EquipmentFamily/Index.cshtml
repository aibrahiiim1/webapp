@model IEnumerable<EquipmentFamily>

@{
    ViewBag.PageTitle = "Equipment Family Management";
    ViewBag.CardTitle = " Equipment Family";
    Layout = "~/Views/Shared/_Dashboard.cshtml";
}
<a class="btn btn-primary" href="/EquipmentFamily/Create">Create</a>
<br />
<table class="table table-hover" width="100%">
    <tr>
        <td>Name</td>
        <td>createdtime</td>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Name</td>
            <td>@item.createdtime</td>
            <td>
                <a class="btn btn-success" href="/EquipmentFamily/Edit/@item.Id">Edit</a>
                <a class="btn btn-danger" href="/EquipmentFamily/Delete/@item.Id">Delete</a>

            </td>
        </tr>
    }
</table>
